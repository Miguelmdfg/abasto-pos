# BodegaFlow POS — Funcionalidades (índice)

Este documento es un índice del alcance implementado, separado en:

## Actual (camino principal)

Ver `docs/FUNCIONALIDADES-ACTUAL.md` (scope del `frontend/` React + Vite).

## Legacy (depreciado)

Ver `docs/FUNCIONALIDADES-LEGACY.md` (scope de `__old/`: API Express legacy + UI estática).
